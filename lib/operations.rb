def unsafe == (speed > 60 || speed < 40)
  unsafe ? "safe" : "not safe"

  

end



def not_safe? == speed
  if not_safe = (speed <= 60 && speed <= 40)
    puts "safe"
  else
    puts "unsafe"
    
	
end
	


